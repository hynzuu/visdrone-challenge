# STEP 3 Configuration - Mixup Augmentation
# Mixup 증강 추가: mixup=0.2

# Model
model: yolov8n.pt

# Training Parameters
epochs: 10
imgsz: 1024
batch: 8
device: cpu

# Random Seed
seed: 42

# Data
data: data.yaml

# Optimization
optimizer: auto
lr0: 0.01
momentum: 0.937
weight_decay: 0.0005

# Augmentation 
mosaic: 1.0
mixup: 0.2 # 추가

# Validation
val: true
save: true
save_period: -1

# Project
project: runs/detect
name: step3_mixup0.2
exist_ok: false
